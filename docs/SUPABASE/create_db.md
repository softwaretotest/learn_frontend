# create db

คำแนะนำในการเลือกปฏิบัติ:หากคุณอยากได้ฐานข้อมูลแยกชื่อ ecom จริงๆ ให้เปิด Query Tool ใน pgAdmin แล้วพิมพ์คำสั่ง

```sql
CREATE DATABASE ecom WITH TEMPLATE = template0;
```

แล้วกดรัน (ปุ่ม Play) ได้เลยครับ จะผ่านทันที
